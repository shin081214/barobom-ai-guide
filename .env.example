# ✅ 필수: Gemini API 키 (Vision 분석 + Live API 실시간 음성)
GEMINI_API_KEY=your_api_key_here

# Gemini 모델 선택 (기본값: gemini-3.5-flash)
# GEMINI_MODEL=gemini-3.5-flash

# 텔레메트리 백엔드 + 기기 식별 API 주소
# NEXT_PUBLIC_API_BASE_URL=https://dev.amanhasfallenintotheriver.space

# future use only: M2+ AI 백엔드 주소 (현재 검증 대상 아님)
# FASTAPI_URL=http://localhost:8000

# future use only: M3+ 데이터 계층 (현재 검증 대상 아님)
# SUPABASE_URL=
# SUPABASE_ANON_KEY=

# future use only: M1 관측성 (현재 검증 대상 아님)
# LANGFUSE_PUBLIC_KEY=
# LANGFUSE_SECRET_KEY=

# 선택 설정 (기본값 사용 가능)
# PORT=4173
